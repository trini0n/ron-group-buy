---
phase: 03-hygiene
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/routes/api/admin/exports/__tests__/exports.test.ts
autonomous: true

must_haves:
  truths:
    - "TODO comments in exports test file are replaced with real assertions"
    - "Export test file has no unwritten test TODOs"
  artifacts:
    - path: "src/routes/api/admin/exports/__tests__/exports.test.ts"
      provides: "Complete test coverage for export content validation"
  key_links: []
---

<objective>
Replace TODO comments in `exports.test.ts` at lines 381 and 389 with real test assertions. The TODOs indicate planned but unwritten assertions for exported file content validation.

Purpose: Tests currently pass trivially at those points — add real assertions to catch export content regressions.
Output: The two TODO locations have concrete `expect(...)` assertions.
</objective>

<context>
@src/routes/api/admin/exports/__tests__/exports.test.ts
@src/lib/server/export-builder.ts
</context>

<tasks>

<task type="auto">
  <name>Implement export content validation assertions</name>
  <files>src/routes/api/admin/exports/__tests__/exports.test.ts</files>
  <action>
    Read the full test file. Find lines 381 and 389 — these are TODO comments indicating missing assertions.

    Read `src/lib/server/export-builder.ts` to understand what the export builder produces (columns, structure, expected content).

    Replace each TODO with a real assertion that validates:
    - The exported file/buffer contains expected column headers (if testing headers)
    - Specific cell values appear in the output (if testing row data)
    - File type/format is correct (e.g. for Excel: row count, sheet name, workbook structure)

    Example pattern for Excel (ExcelJS):
    ```typescript
    expect(workbook.worksheets[0].rowCount).toBeGreaterThan(1); // has data rows
    expect(workbook.worksheets[0].getRow(1).getCell(1).value).toBe('Expected Header');
    ```

    Write assertions that would FAIL if the export builder produced wrong output — not just that it produced *something*.
  </action>
  <verify>
    `npm run test:unit` passes (with the new assertions — not just trivially passing TODOs).
    `grep -n "TODO" src/routes/api/admin/exports/__tests__/exports.test.ts` returns empty.
  </verify>
  <done>Lines 381 and 389 have real assertions. Tests still pass. No TODO comments remain in the file.</done>
</task>

</tasks>

<verification>
```bash
npm run test:unit
grep -n "TODO" src/routes/api/admin/exports/__tests__/exports.test.ts
```
</verification>

<success_criteria>
- Both TODO locations replaced with real expect() assertions
- `npm run test:unit` passes
- New assertions would catch a regression if export builder changed its output format
</success_criteria>

<output>
After completion, create `.gsd/phases/03-hygiene/03-03-SUMMARY.md`
</output>
