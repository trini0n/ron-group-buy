---
phase: 03-hygiene
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/server/notifications/service.ts
autonomous: true

must_haves:
  truths:
    - "`updateNotificationStatus` is either integrated (has callers) or removed"
  artifacts:
    - path: "src/lib/server/notifications/service.ts"
      provides: "No dead exported functions"
  key_links: []
---

<objective>
Remove or integrate `updateNotificationStatus` from `src/lib/server/notifications/service.ts`. Currently exported at line 115 with no callers found in the codebase.

Purpose: Remove dead code that adds maintenance surface with no benefit.
Output: Function removed (if truly unused) or wired up (if it was accidentally disconnected).
</objective>

<context>
@src/lib/server/notifications/service.ts
</context>

<tasks>

<task type="auto">
  <name>Remove or wire updateNotificationStatus</name>
  <files>src/lib/server/notifications/service.ts</files>
  <action>
    First, do a thorough search for usages:
    ```bash
    grep -rn "updateNotificationStatus" src/
    ```

    **If no callers found:** Remove the function from `service.ts`. Remove its export. If there's a corresponding DB operation or type that is only used by this function, remove those too (only if not used elsewhere).

    **If callers exist (false positive from checker):** Leave the function, add a comment explaining its purpose and who calls it. Return what was found.

    Do not remove any other functions — only `updateNotificationStatus` if it has no callers.
  </action>
  <verify>
    `npm run check` passes.
    `grep -rn "updateNotificationStatus" src/` returns empty (or shows the function + its callers if it was wired).
  </verify>
  <done>Dead function removed. Or function confirmed as used and documented.</done>
</task>

</tasks>

<verification>
```bash
npm run check
npm run test:unit
```
</verification>

<success_criteria>
- `updateNotificationStatus` no longer exported without callers
- `npm run check` clean
</success_criteria>

<output>
After completion, create `.gsd/phases/03-hygiene/03-01-SUMMARY.md`
</output>
